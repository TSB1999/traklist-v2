import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  landing: {
    backgroundColor: '#000',
    height: 450,
    borderTopWidth: 2,
    borderColor: 'yellow',
  },
});

export default styles;
